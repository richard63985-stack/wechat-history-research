---
name: wechat-history-research
description: 采集并研究一个微信公众号的可访问历史文章，生成可复核的本地语料库、观点数据库、时间线、主题地图、实体索引和结论化 HTML/PDF 报告。用于全历史公众号梳理、过去一年观点变化或批量研究，不用于只总结单篇文章。
---

# 公众号历史研究

把任务拆成三个独立层级：历史获取、结构化研究、终稿交付。任何一层都不得用下一层的成功掩盖上游缺口。

首次使用、缺少运行环境或更换电脑时，先阅读 [首次运行指南](FIRST_RUN.md)。本仓库提供工作流和基础校验，不内置采集器；根据实际工具能力选择并验证采集路线。

## 先确定边界

至少取得一个种子文章链接、目标公众号名称和用户希望的研究输出。若用户只要求判断能否实现，停在可行性验证；若用户要求完成，继续执行完整闭环。

先区分账号归属：

- 用户自有公众号：经用户授权后可评估官方后台接口或导出文件。
- 第三方公众号：只使用公开页面、用户已登录且有权访问的页面或用户提供的本地导出，不把自有账号接口当成第三方历史接口。

“全部历史”默认解释为当前渠道可访问的历史。平台总量与唯一文章数一致且无分页缺口时，只能说明该渠道目录覆盖完整；没有账号级全量证据，不将其等同于账号绝对全历史。交付优先写明“当前可访问样本”。

## 1. 先跑最小 E2E

用目标公众号的真实链接验证以下两项：

1. 能取得至少一页目录，包含稳定文章 ID、标题和发布日期。
2. 能取得至少一篇正文，并确认不是空白阅读器、登录页或错误页。

优先使用 Codex 内置浏览器或已登录的 Chrome 控制能力。新公众号必须重新验证，不能因为某个仓库 README、旧项目或另一个公众号曾成功，就推断当前仍可用。

若需要选择或核对第三方项目，现场检查官方仓库的维护公告、最近提交、issue、授权方式和上游依赖。验证失败时保留已获得的数据，报告具体断点，不承诺“稳定抓全”。

采集或浏览器认证时，阅读 [跨平台与认证](references/platform-auth.md)。

完成标准：目录页和正文页都已对真实目标跑通，并记录数据源、访问时间和限制。

## 2. 建立可恢复语料库

按以下优先级选择来源：

1. 用户自有账号的官方导出或经授权接口。
2. 用户已登录的微信读书网页目录与阅读器。
3. 用户提供的 HTML、JSON、Markdown 或其他本地归档。
4. 经当前真实 E2E 验证且隐私边界明确的第三方服务。

使用 SQLite 作为事实底稿，原始 JSON/HTML 作为可追溯证据；按稳定文章 ID 幂等写入，支持中断后继续。每篇文章区分 `full`、`summary`、`metadata`、`failed`，正文抓取失败不得用摘要冒充全文。

创建数据库前阅读 [数据契约](references/data-contract.md)，保持字段、状态和 manifest 口径一致。所有路径使用项目相对路径和 `pathlib.Path`，不硬编码用户目录、盘符或路径分隔符。

完成标准：唯一文章无重复；manifest 中的总数、全文数、摘要数、仅元数据数与数据库一致；最早和最晚日期可复核。

## 3. 构建研究层

逐篇生成结构化记录，至少包括：

- 一句话摘要和核心观点；
- 事实、原文/转载机构观点与研究者推断的归属；
- 主题、涉及公司/机构/产品、时间范围；
- 证据等级：全文、摘要或仅元数据；
- 原文链接和稳定文章 ID。

先做文章级结构化，再聚合成：

- 历史观点数据库；
- 核心观点时间线；
- 主题地图；
- 公司、机构与产品索引；
- 最近 12 个月与此前 12 个月的主题变化；
- 当年分阶段的观点演进。

主题和实体抽取应允许多标签，但主主题只能有一个。所有统计分母必须说明是“全部唯一文章”还是“取得全文的文章”。摘要和仅元数据记录可进入目录统计，不能作为强观点证据。

## 4. 需要时做市场验证

只有用户要求验证观点或报告确实需要时才执行。把文章观点改写成可检验的方向性判断，选择对应标的、可解释的基准、文章发布后的首个共同交易日和最新完整交易日，使用复权收盘价计算相对收益。

结果仅标记为：支持、混合、不支持、时间不足、仅作背景。价格同向不是因果证明；公司经营、行业供需和估值结论应另找正式披露或一手来源验证。每一组保留文章链接、价格来源、时间窗口、标的收益、基准收益和相对收益。

## 5. 写成正式终稿

生成 HTML、PDF 和结构化 artifact/JSON。报告结论先行，正文回答“稳定框架是什么、观点如何变化、哪些判断得到验证、哪些仍未验证”。把样本范围、方法和限制集中到附录。

正文不保留“下一步”“待补充”“建议的使用方式”“Further Questions”“TODO”等工作过程表达。原始长摘要改写为结论化观点；事实、转载机构观点、作者选择和本次研究推断分开表述。

构建终稿前阅读 [报告契约](references/report-contract.md)。

## 6. 跨平台输出与验收

HTML 是排版真源，PDF 从同一 HTML 生成。macOS 和 Windows 均使用隔离的临时浏览器配置或现有 PDF 工具，不读取默认浏览器 Cookie 库。PDF 生成后渲染全部页面进行视觉检查。

运行校验脚本：

下面命令从 Skill 目录运行；从研究目录运行时使用实际 Skill 路径，不把研究数据写入 Skill 仓库。

```text
python scripts/validate_workspace.py --db <archive.sqlite> --manifest <manifest.json> --html <report.html>
```

Windows 可使用 `py -3` 替代 `python`；macOS 可使用 `python3`。随后检查：

- PDF 页数和 A4/Letter 设计是否符合用户要求；
- 每页无裁切、横向溢出、黑块、空白异常和表格错位；
- 中文文本层可复制，页码、链接和元数据存在；
- HTML、PDF、JSON 的关键数字一致；
- 旧版文件保留，除非用户明确要求覆盖。

完成标准：采集范围有明确口径，数据库和 manifest 通过脚本校验，报告没有工作备注式表达，PDF 全页视觉检查通过，最终交付文件可直接打开。
